## گزارش ساخت یک دستیار فارسی‌زبان روی داده‌های بورس کالا، و آنچه درباره توصیف ابزار در MCP درست و غلط درآمد
---

> **درباره ترتیب این متن**
>
> این نوشته گزارش یک مسیر است: ساختن یک ابزار مبتنی بر هوش مصنوعی، با خود هوش مصنوعی.
> ترتیب فصل‌ها ترتیب وقوع است: ایده‌ای شکل می‌گیرد، به فرضیه تبدیل می‌شود، تحقیق تأییدش
> می‌کند یا ردش، و بعد کد حرف آخر را می‌زند — گاهی خلاف هر دو.
>
> فرضیه‌های غلط را حذف نکرده‌ام. هر کدام همان‌جا آمده که مطرح شد، همراه با چیزی که بعداً
> معلوم شد. یافته‌های تحقیق را هم از تجربه جدا نکرده‌ام؛ هر ادعا همان‌جایی آمده که در عمل
> به آن برخوردم و مجبور شدم تأیید، تعدیل یا ردش کنم.
>
> مطالب مرجع — نقشه کامل فیلدها، کاتالوگ نمونه‌ها، چک‌لیست و پرامپت ارزیابی — از متن اصلی
> بیرون کشیده شده‌اند و در پیوست‌ها آمده‌اند، تا خط اصلی متن قطع نشود.

---

## فصل یکم — مسئله‌ای که داشبورد حلش نمی‌کرد

سامانه‌ای داشتم که داده‌های رسمی بازار فیزیکی بورس کالای ایران را جمع می‌کرد و به صورت
داشبورد نشان می‌داد. محدودیتش ساختاری بود، نه اجرایی: **داشبورد فقط به پرسش‌هایی پاسخ
می‌دهد که طراحش از پیش تعریف کرده است.**

کاربری که می‌پرسد «این کارگزار در سه ماه گذشته با چه عرضه‌کنندگانی کار کرده و سهمش از هر
گروه کالایی چقدر بوده؟» پرسشی دارد در تقاطع سه موضوع مختلف: کارگزار، عرضه‌کننده و درخت
کالایی، که در هیچ‌کدام کامل نیست.

گزینه متعارف، صفحه بیشتر است. مشکل این است که فضای پرسش‌های ممکن نمایی رشد می‌کند و فضای
موضوعات خطی. از جایی به بعد، هر موضوع تازه فقط شکاف بعدی را جابه‌جا می‌کند.

پس تصمیم گرفتم همان سرویس‌های موجود برنامه را به مدل بدهم و اجازه دهم کاربر مستقیم بپرسد.
رابط پرسش‌محور یک ویژگی ناخوشایند دارد که باید از ابتدا پذیرفت: **خروجی قطعی نیست.** یک
کامپوننت همیشه همان را نشان می‌دهد که کوئری‌اش برمی‌گرداند؛ یک مدل زبانی ممکن است دو بار به
یک پرسش دو جور پاسخ دهد.

از اینجا به بعد، کل کار معماری محدود کردن دامنه همین عدم‌قطعیت است. و اولین ایده‌ای که برای
محدود کردنش به ذهنم رسید، غلط بود.

---

## فصل دوم — نخستین فرضیه: زبان

### ایده چطور شکل گرفت

دو واقعیت را کنار هم گذاشتم. اول اینکه کاربران من فارسی‌زبانند و پرسش‌هایشان را فارسی
می‌نویسند. دوم اینکه مدل من — به دلیلی که در فصل چهارم می‌گویم — در فارسی قوی نیست.

از این دو، نتیجه‌ای گرفتم که در لحظه بدیهی به نظر می‌رسید:

> **فرضیه اول:** اگر توصیف سرور MCP و متدهایش را فارسی بنویسم، تطبیق معنایی بهتری بین پرسش
> فارسی کاربر و ابزار رخ می‌دهد؛ هم خود سرور بهتر پیدا می‌شود و هم متدهایش.

مدل ذهنی پشت این فرضیه، **شباهت بردار معنایی** بود: پرسش فارسی و توصیف فارسی در یک فضا، پس
نزدیک‌ترند. اگر سازوکار انتخاب ابزار یک بازیابی برداری خام بود، فرضیه درست بود.

### چرا غلط بود

سازوکار این نیست. در MCP، فیلد `description` و طرحواره ابزارها **مستقیم داخل زمینه مدل**
قرار می‌گیرند تا مدل سه کار کند: انتخاب ابزار، پر کردن درست پارامترها، و تشخیص اینکه چه
زمانی **نباید** از ابزار استفاده کند. این یک بازیابی نیست؛ یک استدلال درون‌متنی است. و
مدل‌هایی که امروز با MCP کار می‌کنند، هسته استدلال فراخوانی ابزارشان روی داده انگلیسی شکل
گرفته است.

تحقیق سه چیز نشان داد که هر سه خلاف فرضیه بودند:

**یک: افت عملکرد واقعی است، اما هزینه زبان کاربر است نه زبان توصیف.** وقتی کاربر به زبان
غیر انگلیسی صحبت می‌کند، دقت فراخوانی ابزار به‌طور میانگین پنج تا ده درصد و در موارد شدیدتر
بیشتر افت می‌کند. این افت **تقریباً مستقل از زبان توصیف ابزارها** رخ می‌دهد. یعنی چیزی که
فرضیه می‌خواست درمانش کند، اصلاً علتش آن نبود.

**دو: آزمایش کنترل‌شده، جهت عکس را نشان داد.** در یک بنچمارک عربی مبتنی بر BFCL، توصیف
انگلیسی به‌طور معنی‌داری بهتر از توصیف عربی عمل کرد — سیصد و بیست‌ودو پیروزی در برابر
سی‌وهفت. و ترکیب «پرامپت عربی + توصیف عربی» بدترین نتیجه ممکن را داد؛ چیزی که در متون این
حوزه **جریمه زبانی انباشته** نامیده می‌شود. یعنی فرضیه من نه فقط بی‌اثر، که مشخصاً مضر بود.

**سه: شکل غالب شکست جای دیگری بود.** پرتکرارترین خطا در سامانه‌های چندزبانه این است که مدل
نیت را درست می‌فهمد، ابزار درست را هم انتخاب می‌کند، اما **مقدار پارامتر** را به زبان کاربر
تولید می‌کند — نام شهر یا مقدار `enum` به فارسی — در حالی که بک‌اند انگلیسی می‌خواهد. مشکل
در تولید خروجی ساختاریافته است، نه در درک معنایی. فارسی کردن توصیف این را حل نمی‌کند؛
تشدیدش می‌کند، چون به مدل سیگنال می‌دهد که فضای فارسی فضای درستی است.

دو نکته فرعی هم بود که خودم باید زودتر می‌دیدم: نام ابزار و پارامترها در هر حالت انگلیسی
می‌مانند، پس توصیف فارسی روی شناسه‌های انگلیسی متنی دوپاره می‌سازد؛ و در رجیستری‌ها و
فهرست‌های سرور، انگلیسی استاندارد عملی است.

### وضعیت فرضیه

**رد شد — پیش از آنکه یک خط کد بنویسم.** این ارزان‌ترین شکل رد یک فرضیه است و تنها موردی
در این پروژه که تحقیق توانست جلوی هدررفت زمان را بگیرد.

جایگزینش ساده بود: توصیف‌ها انگلیسی باکیفیت، شناسه‌ها انگلیسی ثابت، و اگر لازم شد یک
راهنمایی چندزبانه در `system prompt`. استثناهایش هم واقعی ولی بسیار محدود است — مدلی که
اختصاصاً روی فارسی تنظیم شده باشد، یا آزمون A/B با بهبود قابل اندازه‌گیری. هیچ‌کدام حالت من
نبود.

اما مسئله زبان اینجا بسته نشد. فرضیه رد شد، ولی خود **مسئله** سر جایش ماند؛ فقط معلوم شد در
جای دیگری نشسته. جواب واقعی‌اش را در فصل دوازدهم پیدا کردم، و درست صد و هشتاد درجه برعکس
چیزی بود که در این فصل حدس زده بودم.

---

## فصل سوم — مخاطب این متن، انسان نیست

رد فرضیه اول یک سؤال بزرگ‌تر باقی گذاشت: اگر تطبیق زبانی جواب کار نیست، پس چه چیزی تعیین
می‌کند مدل ابزار درست را انتخاب کند؟ اینجا بود که فهمیدم عادت برنامه‌نویسی کلاسیکم دارد
گمراهم می‌کند.

سال‌ها با این سه فرض کد نوشته بودم: کامپایلر امضای تابع را چک می‌کند؛ مستندات برای
**خواننده انسانی** نوشته می‌شود؛ ورودی اشتباه به استثنا ختم می‌شود. در MCP هر سه می‌شکند.
مخاطب متنی که می‌نویسم یک انسان باهوش نیست — یک موتور احتمالاتی است که در هر نوبت تمام
توصیف‌ها را می‌خواند تا تصمیم بگیرد چه چیزی را با چه پارامترهایی صدا بزند.

> **بازتعریف:** توصیف ابزار در MCP مستندات نیست؛ یک **دستورالعمل پرامپت در زمان اجرا** است
> که هزینه توکن دارد و مستقیم روی رفتار اثر می‌گذارد.

### چه چیزهایی را باید چک می‌کردم

بررسی تجربی روی هشتصد و پنجاه‌وشش ابزار در صد و سه سرور واقعی MCP نشان می‌دهد ۹۷٫۱٪ ابزارها
دست‌کم یک ایراد کیفی دارند و ۵۶٪ آن‌ها حتی هدف اصلی ابزار را شفاف بیان نکرده‌اند. شش ایراد
پرتکرار:

| ایراد | شیوع | مشکل |
|---|---|---|
| محدودیت‌های بیان‌نشده | ۸۹٫۸٪ | مرزها، سقف نرخ و رفتار حالت‌های مرزی گفته نشده |
| نبود راهنمای استفاده | ۸۹٫۳٪ | شرط تحریک و شرط عدم‌استفاده مشخص نیست |
| پارامترهای مبهم | ۸۴٫۳٪ | اکتفا به `string` یا `object` بدون فرمت و مقادیر مجاز |
| توصیف ناقص | ۷۹٫۱٪ | عبارت‌های دوکلمه‌ای مثل «Gets commits» |
| مشکل مثال‌ها | ۷۷٫۹٪ | مثال نامعتبر، یا مثال به‌جای شرح متنی |
| هدف نامشخص | ۵۶٫۰٪ | فعل و شیء عملیات معلوم نیست |

و سه شکل شکست که این ایرادها تولید می‌کنند:

**عدم فراخوانی.** مدل پاسخ را توهم می‌کند یا از کاربر سؤال می‌پرسد، در حالی که ابزار مناسب
موجود است. علتش معمولاً این است که توصیف به‌صورت **اسم** نوشته شده — «سرویس آب‌وهوا» — نه
به‌صورت **شرط تحریک**.

**سوءاستفاده با اطمینان بالا.** ابزار درست صدا زده می‌شود ولی پارامترها بدقالب‌اند. طرحواره
فقط `string` را الزام کرده و متن، فرمت واقعی را نگفته.

**انتخاب ابزار اشتباه.** مدل میان دو ابزار همسایه اشتباه انتخاب می‌کند، چون مرزهای نفی نوشته
نشده.

این سه را اینجا فهرست می‌کنم چون دو تایشان را در ادامه از نزدیک دیدم: انتخاب ابزار اشتباه دو
بار و به دو علت کاملاً متفاوت (فصل‌های چهارم و ششم)، و سوءاستفاده با اطمینان بالا در شکلی
که از خود تحقیق هم خطرناک‌تر بود (فصل هفتم).

تحقیق درست پیش‌بینی کرده بود که **چه** اتفاقی می‌افتد. چیزی که پیش‌بینی نکرده بود، **علت
واقعی** آن اتفاق‌ها در پروژه من بود — و در هر دو مورد، علت جایی بود که تحقیق اصلاً نگاه
نمی‌کرد.

---

## فصل چهارم — تصمیم اول در کد: چهل‌ودو ابزار

### دو راه ممکن

برای دسترسی مدل به داده، هر قابلیت به‌صورت تابعی قابل فراخوانی تعریف می‌شود و مدل انتخاب
می‌کند کدام را صدا بزند. سر دانه‌بندی یک معامله روشن هست:

- **سطح گسترده** — هر متد سرویس یک ابزار. هیچ پرسشی بی‌پاسخ نمی‌ماند، ولی مدل باید میان
  ده‌ها گزینه انتخاب کند.
- **سطح فشرده** — چند ابزار درشت‌دانه با پارامتر. انتخاب ساده‌تر، خروجی سنگین‌تر، و بخشی از
  داده دسترس‌ناپذیر.

راه دوم اسم و الگوی مشخصی دارد: **استاندارد جستجو و دریافت**. در این الگو یک ابزار `search`
می‌گذارید که تنها یک پارامتر رشته‌ای می‌گیرد، ولی توصیفش به مدل می‌آموزد که هم زبان طبیعی و
هم نحو ساختاریافته را می‌پذیرد؛ و یک ابزار `fetch` که تنها یک شناسه می‌گیرد و از روی ساختار
خود شناسه مسیریابی می‌کند. کل سرور می‌تواند دو ابزار داشته باشد.

من راه اول را برداشتم: **چهل‌ودو ابزار.** دلیلم این بود که سرویس‌های برنامه از قبل وجود
داشتند و هر کدام یک پرسش واقعی کاربر را جواب می‌دادند؛ فشرده کردنشان یعنی دور ریختن
تفکیک‌هایی که خود دامنه ساخته بود.

### هزینه، در نخستین آزمون

مدل برای پرسشی درباره یک کارگزار، چهار ابزار بی‌ربط بازار را صدا زد. همان **انتخاب ابزار
اشتباه** فصل قبل، این بار نه به خاطر توصیف بد، که به خاطر شلوغی فضای انتخاب.

عددی که در تحقیق دیده بودم اینجا معنا پیدا کرد: غنی کردن توصیف‌ها نرخ موفقیت مأموریت را
حدود شش درصد و امتیاز کیفیت پاسخ را حدود پانزده درصد بالا می‌برد، اما تعداد گام‌های اجرا و
مصرف توکن را حدود **۶۷ درصد**. من این هزینه را دو بار پرداخت کرده بودم: یک بار برای غنای
متن، یک بار برای تعداد ابزار.

ولی مهم‌تر از هزینه، فرضی بود که زیر این تصمیم نشسته بود و غلط بود. فرض کرده بودم «اگر ابزاری
نباشد، پاسخی نیست». گزاره درست این است:

> **ابزاری که پاسخ غلط می‌دهد از نبود ابزار بدتر است.** نبود داده به «نمی‌دانم» ترجمه
> می‌شود؛ داده غلط به یک پاسخ مطمئن.

این جمله بعداً پنج ابزار را بازنشسته کرد (فصل یازدهم).

### قیدی که متغیر نبود

مدلی که استفاده می‌کنم رایگان است و در ردیف مدل‌های قدرتمند نیست. این قید بودجه است، نه
انتخاب فنی — و تغییرش دست من نبود.

قیدی که نمی‌شود عوضش کرد، صورت مسئله را عوض می‌کند. تا وقتی مدل را متغیر می‌دیدم، هر خطا
توضیح آسانی داشت و هیچ کاری لازم نبود جز «مدل بهتر». وقتی پذیرفتم ثابت است، فهرست متغیرهایم
کوتاه شد و همه‌شان در کنترل من بودند: **چه داده‌ای می‌دهم، با چه نامی، با چه توضیحی، و چه
چیزی را اصلاً به او واگذار نمی‌کنم.**

معلوم شد همین کافی است. ولی برای رسیدن به آن، باید یک اشتباه مفهومی دیگر را هم تصحیح
می‌کردم.

---

## فصل پنجم — چه کسی این فیلد را می‌خواند؟

### فرضیه دوم

در میانه کار به نتیجه‌ای رسیدم که راه‌حل مسئله چندزبانگی به نظر می‌رسید:

> **فرضیه دوم:** در MCP باید از `title` و `annotations` استفاده کرد. نام فارسی خوانا در
> `title`، عنوان کوتاه فارسی در `annotations.title`، و `title` فارسی داخل هر پارامتر.
> این‌طور هم مدل توصیف انگلیسی قوی دارد و هم کاربر فارسی متن فارسی می‌بیند.

نیمه دوم این جمله درست است. نیمه اولش یک خطای دسته‌بندی بود.

### تصحیح

بعداً — و پس از آشنایی با تفکیک **پارامترهایی که کاربر با آن‌ها تعامل می‌کند** از
**پارامترهایی که مدل با آن‌ها تعامل می‌کند** — روشن شد که `title` و `annotations` مشخصاً
برای تعامل کاربر و کلاینت ساخته شده‌اند، نه برای مسیر تصمیم‌گیری مدل:

- `title` و `annotations.title` **نام نمایشی‌اند**. کلاینت آن‌ها را در رابط کاربری نشان
  می‌دهد. فارسی نوشتنشان کار درستی است، ولی **کشف ابزار توسط مدل را بهتر نمی‌کند.**
- `readOnlyHint`، `destructiveHint`، `idempotentHint` و `openWorldHint` **راهنمای رفتاری
  برای کلاینت‌اند** — مثلاً گرفتن تأیید پیش از عملیات مخرب. این‌ها اهرم ایمنی و تجربه
  کاربری‌اند، نه اهرم دقت مدل. ضمناً از منظر پروتکل «راهنما» محسوب می‌شوند و برای تصمیم‌های
  امنیتی سخت نباید به آن‌ها اتکای مطلق کرد.

پس تنها اهرمی که مستقیم روی رفتار مدل اثر می‌گذارد `description` است — در سطح ابزار و در سطح
تک‌تک پارامترها — به‌علاوه خود شناسه‌ها و ساختار طرحواره. فرضیه دوم پاسخ درستی بود به سؤالی
که نپرسیده بودم.

درس عام‌ترش این است: **پیش از بهینه‌سازی یک فیلد، مطمئن شو چه کسی آن را می‌خواند.**

و همین درس، مدتی بعد، در جای دیگری تکرار شد — این بار نه در تعریف ابزار، که در خروجی‌اش.

### همان سؤال، یک لایه پایین‌تر

اولین بازبینی جدی را نه روی پاسخ‌های مدل، که روی **ورودی‌اش** انجام دادم: همان JSON که
ابزارها تحویل می‌دادند. ابزارها داده خام برنمی‌گرداندند؛ **مدل‌های ارائه** برمی‌گرداندند،
یعنی شیءهایی که برای ویجت‌های داشبورد طراحی شده بودند:

```json
{ "title": "پتانسیل بازار", "value": "12.3 همت",
  "iconCssClass": "bi bi-cash-stack", "changeState": 1 }
```

سه مسئله متفاوت قاطی شده‌اند. **نویز ظاهری**، که جز مصرف توکن کاری نمی‌کند. **ساختار برچسب
و مقدار**، که مدل باید دو فیلد را کنار هم بگذارد تا یک گزاره بسازد. و از همه مهم‌تر، **عنوان
و مقدار یکی نبودند**: عنوان می‌گفت «پتانسیل بازار» و عدد، ارزش معامله‌شده بود.

برای کارتی که یک‌جا دیده می‌شود و زیرنویس دارد، این فشرده‌سازی قابل دفاعی است؛ برای
مصرف‌کننده‌ای که آن را جفت کلید و مقدار می‌بیند، گزاره‌ای نادرست.

> **نتیجه معمارانه:** ویجت و مدل دو مصرف‌کننده با دو قرارداد متفاوت‌اند. یکی زمینه دارد —
> کارت، رنگ، مجاورت، دانش کاربر — دیگری ندارد. اشتراک مدل داده میان این دو صرفه‌جویی نیست؛
> انتقال ابهام است.

دقیقاً همان تفکیکی که در نیمه اول این فصل درباره `title` و `description` یاد گرفته بودم،
اینجا درباره خروجی صادق بود. یک اصل، دو محل:

| مخاطب | چه چیزی را می‌خواند | خطا در آن چه می‌کند |
|---|---|---|
| پروتکل و کد | `name`، کلیدهای `properties`، مقادیر `enum`، `required` | فراخوانی می‌شکند |
| مدل | `description` ابزار و پارامترها، و کل خروجی ابزار | دقت انتخاب و مقداردهی افت می‌کند |
| انسان | `title`، `annotations.title`، `title` پراپرتی، و متن نهایی پاسخ | خوانایی افت می‌کند؛ اثری بر مدل ندارد |

و قانون زبانی که از آن درآمد و تا آخر پروژه دوام آورد:

| دسته | زبان | دلیل |
|---|---|---|
| شناسه‌های ماشینی (`name`، نام پارامترها، مقادیر `enum`) | حتماً انگلیسی | مدل و پروتکل با این‌ها کار می‌کنند؛ فارسی کردنشان فراخوانی را می‌شکند |
| متن توضیحی برای مدل (`description`) | انگلیسی قوی | مدل‌ها روی انگلیسی بهتر استدلال می‌کنند |
| متن نمایشی برای انسان (`title`ها) | فارسی یا دوزبانه | فقط برای رابط کاربری |

نقشه کامل فیلدها و تصمیم زبان هرکدام در **پیوست الف** آمده است.

---

## فصل ششم — نام، پیش از توضیح خوانده می‌شود

از اینجا بازنگری ابزار به ابزار شروع شد. اولین چیزی که خودش را نشان داد، جایی بود که اصلاً
انتظارش را نداشتم.

تابعی داشتم به نام `GetMarketSentiment` که ترکیب **نوع قرارداد** را برمی‌گرداند — نقدی،
سلف، نسیه. توضیحش درست بود. عنوان فارسی‌اش هم درست بود. فقط نامش غلط بود.

«سنتیمنت بازار» در عرف یعنی صعودی یا نزولی بودن. مدل میان ده‌ها ابزار اول از روی نام غربال
می‌کند: برای پرسش «بازار مثبت بود یا منفی؟» این را برمی‌داشت و شرایط پرداخت تحویل می‌داد —
پاسخی که چون عدد دارد، قانع‌کننده به نظر می‌رسد.

تحقیق گفته بود نام باید ترکیب فعل و شیء مشخص باشد و از اسامی عمومی مثل `run` و `process`
پرهیز شود. این توصیه درست بود، ولی تجربه یک درجه تشدیدش کرد:

> **نام غلط، توصیف درست را بی‌اثر می‌کند.** غربال اول مدل روی نام است، نه روی متن. تا وقتی
> نام اشتباه است، هر چقدر `description` را دقیق‌تر کنید فرقی نمی‌کند — چون نوبت به آن
> نمی‌رسد.

نام **بی‌معنا** هم کم‌ضررتر نیست، فقط دیرتر خودش را نشان می‌دهد: ابزاری داشتم به نام
«گروه‌های شاخص» که صرفاً پنج گروه پرعرضه روز را برمی‌گرداند. نه در داده شاخصی بود، نه در
کسب‌وکار چنین مفهومی داریم. مدل نه می‌دانست کی صدایش بزند و نه کی نزند.

### نام بدون شناسه، مدل را به جعل می‌کشاند

در برنامه عرضه عرضه‌کننده، نام کارگزار در خروجی بود ولی شناسه‌اش نه. پرامپت صریح می‌گفت
«اگر نشانی نیامده بود، نام را ساده بنویس». مدل به‌جایش نشانی جعلی ساخت:

```
[بانک رفاه کارگران](/players/brokers/???)
```

شناسه در همان کوئری موجود بود و فقط دور ریخته می‌شد. با افزودنش، آزمون بعدی صفر نشانی جعلی
داد و پانزده پیوند درست.

**درس:** وقتی نام یک موجودیت را می‌دهی، شناسه‌اش را هم بده. نام تنها کافی نیست؛ قاعده «شناسه
نساز» در برابرش دوام نمی‌آورد. این اولین نشانه‌ی چیزی بود که در فصل هشتم عدد پیدا کرد:
قاعده متنی در برابر ساختار داده همیشه بازنده است.

و بدتر از شناسه نداشته: **شناسه‌ای که به چیز دیگری اشاره می‌کند.** در فهرست پویاترین‌ها،
عنوان ردیف نام زیرگروه بود و شناسه‌اش شناسه کالا. پیوندی که ساخته می‌شد معتبر بود، باز
می‌شد، و غلط بود — بی‌آنکه از بیرون قابل تشخیص باشد.

---

## فصل هفتم — سه حالتی که یکی شده بودند

پرتکرارترین الگوی کل پروژه این بود، و در چهار شکل مستقل ظاهر شد. همه‌شان یک هسته دارند:
**«نداریم»، «صفر» و «ورودی غلط» در خروجی از هم تفکیک‌ناپذیر شده بودند.**

**پیش‌فرض حدسی.** مدل عرضه مقدار پیش‌فرض `CurrencyUnit = "ریال"` داشت. ولی قیمت‌ها همیشه
ریالی نیستند: در داده ما نُه ارز شمرده شده — ریال، دلار، درهم، یورو، یکصد ین، دلار آزاد،
یورو آزاد و ریال صادراتی. عرضه‌های صادراتی دلاری‌اند. مدل قوی شاید از بافت بفهمد؛ مدل ضعیف
عدد را با واحد غلط چاپ می‌کند.

> وقتی مقدار واقعی را نمی‌دانی، فیلد را حذف کن. مدل نمی‌تواند چیزی را ادعا کند که در داده
> نیست، ولی پیش‌فرض قابل قبول را حتماً باور می‌کند.

**صفری که از فرمول بیرون می‌آید.** الگوی `x > 0 ? a / x : 0` برای جلوگیری از تقسیم بر صفر
کاملاً درست است. ولی آن صفر، از «هیچ داده‌ای نبود» تفکیک‌ناپذیر است. در روزی که اصلاً
عرضه‌ای ثبت نشده بود، خروجی می‌گفت «۰٪ عرضه‌ها معامله شد» — جمله‌ای که شکل اندازه‌گیری دارد
و پشتش اندازه‌گیری‌ای نیست.

**ورودی بدقالب.** تاریخ به‌صورت رشته مقایسه می‌شد. اگر مدل «۱۴۰۴/۶/۱۰» می‌فرستاد — بدون صفر
— یا «۱۴۰۴۰۶۱۰» که قالب دیگر همین پروژه است، هیچ ردیفی تطبیق نمی‌کرد و پاسخ می‌شد «برای این
تاریخ عرضه‌ای ثبت نشده است». یعنی **خطای قالب ورودی، به شکل واقعیت بازار گزارش می‌شد.**

این دقیقاً همان «سوءاستفاده با اطمینان بالا»ی فصل سوم است، ولی با تشدیدی که در تحقیق ندیده
بودم: وقتی خروجی هم مبهم باشد، خطا **نامرئی** می‌شود. مدل اشتباه کرده، سیستم سکوت کرده، و
کاربر یک گزاره تحلیلی گرفته است.

> برای هر پارامتر رشته‌ای سه حالت وجود دارد: معتبر و پُر، معتبر و خالی، نامعتبر. اگر کد فقط
> دو تا را بشناسد، سومی خودش را به‌جای بی‌ضررترینشان جا می‌زند.

**ترتیب بررسی.** در روند قیمت کالا، خالی بودن نتیجه پیش از وجود خود کالا بررسی می‌شد. شناسه
ناموجود و کالای بی‌معامله هر دو به یک خروجی می‌رسیدند و مدل می‌گفت «این کالا در این بازه
معامله‌ای نداشته» — درباره کالایی که وجود نداشت. موجودیت را همیشه اول تأیید کن.

### شکل دیگر همین ابهام: برچسب و مقدار

ویجت‌های آمار ساختار `{label: "سهم از تعداد", value: "۳۵٫۲٪"}` داشتند. برای کارت طبیعی است؛
برای مدل یک کار اضافه با فرصت خطا. وقتی برچسب **خود کلید** باشد، جایی برای اشتباه نمی‌ماند.

حالت سخت‌تر وقتی است که **معنای یک فیلد به مقدار فیلد دیگری وابسته است**: در فهرست
زیرمجموعه‌ها، `offerCount` اگر وضعیت «امروز فعال» بود شمار عرضه‌های امروز بود و اگر «فعال در
آینده» بود شمار عرضه‌های پیش‌رو. این وابستگی فقط در کد سرویس وجود داشت و در هیچ توصیفی قابل
جبران نبود. جایگزینش یک جمله آماده شد: «امروز ۳ عرضه دارد».

---

## فصل هشتم — کد یا پرامپت: جایی که درستی را می‌گذاریم

این پرسش محور پروژه شد، و برخلاف بقیه، عدد دارد.

پس از یک دور اصلاح، پنج پرسش پیاپی را سنجیدم و دو نوع مداخله را با هم مقایسه کردم:

- **اصلاح ساختاری** — حذف فیلد، کلید فارسی، جمله آماده به‌جای دو فیلد جدا — تقریباً همیشه
  جواب می‌دهد، چون تصمیمی برای مدل باقی نمی‌گذارد.
- **قاعده رفتاری در پرامپت** — «فارسی بنویس»، «واحد را تبدیل نکن» — حدود **هشتاد تا نود
  درصد** جواب می‌دهد و بین اجراها نوسان دارد.

برای یک ابزار سرگرمی، تفاوت ۸۵٪ و ۱۰۰٪ ناچیز است. برای گزارشی که ممکن است مبنای تصمیم مالی
قرار بگیرد، همه‌چیز است.

### قاعده منفی را مدل ضعیف «ممنوعیت» می‌خواند

بین همه‌ی چیزهایی که در این پروژه یاد گرفتم، این یکی بیشترین درس را داشت — و مستقیم یکی از
توصیه‌های تحقیق را تعدیل کرد.

پیوندهای مارک‌داون در **پنج آزمون پیاپی** ساخته نشدند، با اینکه فیلد نشانی در خروجی بود و هم
پرامپت و هم توصیف ابزار آن را خواسته بودند. نزدیک بود نتیجه بگیرم مدل از پس این کار
برنمی‌آید. علت این جمله بود، در پرامپت خودم:

> «هر مورد حداکثر یک بار پیوند شود؛ پیوند به همان مورد را در **جدول‌ها و فهرست‌ها** تکرار نکن.»

منظورم «تکرار نکن» بود. مدل خواند «در جدول و فهرست پیوند نگذار» — و تقریباً همه پاسخ‌هایش
جدول و فهرست است. جمله را مثبت کردم — «**اولین** ذکر هر مورد پیوند شود» — و تصریح کردم که
شامل داخل جدول‌ها هم می‌شود. پیوندها بلافاصله برگشتند.

تحقیق گفته بود که برای تفکیک ابزارهای همسایه باید صریح نوشت **چه زمانی نباید** از ابزار
استفاده شود. این توصیه برای مدل‌های قوی درست است. تجربه من مشروطش کرد:

> **قاعده منفی را با دامنه دقیق و ترجیحاً در قالب مثبت بنویس.** «برای X از `tool_b` استفاده
> کن» بهتر از «برای X از این ابزار استفاده نکن» است، چون دومی مستعد تعمیم بیش از حد است و
> مدل ضعیف آن را به یک ممنوعیت عام ترجمه می‌کند.

و درس عمومی‌ترش: اگر قاعده‌ای اجرا نشد، پیش از آنکه آن را ضعف مدل بدانی، دنبال جمله‌ای بگرد
که خلافش را می‌گوید.

### هر هشدار، یک پیش‌بینی خطاست

از همین خانواده بود مورد دیگری. توصیف یکی از ابزارها می‌گفت «این شناسه با شناسه عرضه فرق
دارد» — یعنی من دقیقاً می‌دانستم محتمل‌ترین اشتباه چیست و آن را نوشته بودم. ولی وقتی آن
اشتباه رخ می‌داد، خروجی فقط می‌گفت «یافت نشد» و مدل نتیجه می‌گرفت «این عرضه معامله نشده».

هشدار کار خودش را می‌کرد، ولی نه همیشه؛ و وقتی نمی‌کرد، هیچ‌چیز جلویش را نمی‌گرفت. حالا کد
بررسی می‌کند آیا عرضه‌ای با آن شناسه هست و صریح می‌گوید شناسه از فضای دیگری آمده.

> **هر هشداری که در توصیف ابزار می‌نویسی، یعنی محتمل‌ترین خطا را می‌شناسی.** و چیزی که
> می‌شناسی را می‌شود در کد تشخیص داد. هشدار متنی هشتاد تا نود درصد کار می‌کند؛ تشخیص در کد،
> همیشه.

### قاعده‌ای که از این فصل درآمد

اینجا بود که تناقض ظاهری بین تحقیق و تجربه حل شد. تحقیق می‌گفت «توصیف را غنی کن»؛ تجربه
می‌گفت «هر چه در کد قطعی‌شدنی است در کد قطعی کن». این دو با هم در تضاد نیستند، اگر بدانیم
هرکدام کجا می‌نشیند:

| لایه                           | چه چیزی آنجا می‌نشیند                                                                                                     | نوع تضمین                                   |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| طرحواره                        | تایپ، `enum`، `required`، کران‌ها، `pattern`، `default`                                                                   | قطعی و ماشینی؛ مدل عملاً نمی‌تواند نقضش کند |
| کد (سرویس و لایه سریال‌سازی)   | اعتبارسنجی ورودی، تفکیک «نداریم» از «صفر»، واحد داخل مقدار، جمله به‌جای بولین، حذف نویز ظاهری، تشخیص شناسه بیگانه         | قطعی؛ صد درصد                               |
| توصیف (`description`)          | هدف، شرط تحریک، تمایز با ابزار همسایه، نگاشت واژه‌ها، شکل خروجی                                                           | احتمالاتی؛ هشتاد تا نود درصد و نوسان‌دار    |

**قاعده:** هر چیزی را تا جای ممکن یک لایه بالاتر ببر. اگر می‌شود در طرحواره بست، در توصیف
ننویس. اگر می‌شود در کد قطعی کرد، به پرامپت نسپار. `description` جای چیزهایی است که جای
دیگری ندارند — عمدتاً قضاوت درباره اینکه کی این ابزار مناسب است.

عملاً یعنی: واحد داخل مقدار، جمله به‌جای بولین، خلاصه روند از پیش محاسبه‌شده، و یک جمله
صریح که می‌گوید داده مربوط به امروز نیست. آنچه برای مدل می‌ماند همان است که در آن خوب است:
انتخاب ابزار، ترکیب قطعه‌ها، و نگارش روان.

---

## فصل نهم — قاعده سراسری و استثناهای نامرئی

قواعد عمومی را در **لایه سریال‌سازی** گذاشتم، نه در تک‌تک ابزارها: حذف فیلدهای ظاهری، برچسب
فارسی برای `enum`ها، گرد کردن اعشار، حذف رشته و مجموعه خالی — ولی نگه‌داشتن **صفر عددی**،
چون «صفر معامله» خودش یک واقعیت است.

مزیتش این است که ابزار تازه نمی‌تواند قاعده را فراموش کند. ولی سه درس متقابل داد.

**فیلتر را با قاعده عام بنویس، نه با فهرست نمونه‌ها.** فیلتر فیلدهای ظاهری «نامی که به
`CssClass` ختم شود» را می‌گرفت. سه بار سوراخ پیدا کرد: `colorCssVariable`، `iconColorClass`،
`iconSvg`. هر بار ماه‌ها بی‌سروصدا کد رنگ به مدل رفته بود.

**ولی قاعده عام هم حد دارد.** وسوسه شدم «هر نامی که به `Class` ختم شود» را حذف کنم؛
`StrengthClass` در مشخصات سیمان یک رده فنی واقعی است. تعمیمی که یک چیز را درست می‌کند و چیز
دیگری را می‌شکند، تعمیم نیست.

**و خطرناک‌ترین.** برای زیبا کردن خروجی می‌خواستم به همه `enum`ها مبدل فارسی بدهم. یکی از
آن‌ها **پارامتر ورودی** هم بود. مبدل در هر دو جهت کار می‌کند و هنگام خواندن، برچسب فارسی در
`Enum.TryParse` شکست می‌خورد و بی‌صدا مقدار پیش‌فرض را برمی‌گرداند — یعنی «جستجوی کارگزار»
بی‌هیچ خطایی به «جستجوی کالا» تبدیل می‌شد.

این همان قانونی است که در فصل پنجم نوشتم: مقادیر `enum` شناسه ماشینی‌اند و باید انگلیسی
بمانند. تازگی‌اش این بود که **نقض آن لازم نیست عمدی باشد.** من نمی‌خواستم `enum` را فارسی
کنم؛ می‌خواستم خروجی را زیبا کنم. یک مبدل نمایشی که ناخواسته دوطرفه شده بود کافی بود. زیبا
کردن خروجی نباید ورودی را خراب کند.

و درس سوم، که عمومی‌تر از این پروژه است:

> **بعد از هر اصلاح سراسری، سراغ ابزارهایی برو که مشمولش نشده‌اند**، نه آن‌هایی که شده‌اند.
> قاعده‌ای که ۹۰٪ موارد را پوشش می‌دهد، ۱۰٪ باقی را خطرناک‌تر می‌کند، چون همه — از جمله
> خودت — فرض می‌کنند پوشش کامل است.

---

## فصل دهم — آنچه دستیار نشان داد

نکته‌ای که انتظارش را نداشتم: **بیشتر ایرادها از پیش آنجا بودند و دستیار فقط نشانشان داد.**
داشبورد سال‌ها همان اعداد را نشان داده بود، در قالبی که خطا در آن به چشم نمی‌آمد. مدل، اولین
مصرف‌کننده‌ای بود که زمینه نداشت — و بی‌زمینه بودنش کار یک ابزار تشخیص را کرد.

**ضریب واحد.** تبدیل به «همت» ضریبی ده برابر خطا داشت. یک همت هزار میلیارد تومان است، یعنی
ده هزار میلیارد ریال؛ گام ریال به تومان جا افتاده بود. عدد بزرگ، بزرگ به نظر می‌رسد. بدتر
اینکه مستند داخلی پروژه همان تابع معیوب را «الگوی درست» معرفی کرده بود.

**جمع روی سمت «یک» در یک اتصال یک‌به‌چند.** «نسبت فروش به عرضه»ی هر تالار با جمع‌زدن حجم
عرضه روی ردیف‌های گزارش معامله حساب می‌شد؛ هر عرضه می‌تواند چند گزارش داشته باشد، پس مخرج
چند برابر می‌شد. نتیجه سه تا هفت درصد بود — عددی که چون «بازار ضعیف» تفسیرپذیر است، هرگز به
آن شک نشده بود. همین اشتباه بعداً در دو ابزار دیگر هم پیدا شد؛ بار سوم دیگر «چند مورد» نبود،
ویژگی مدل داده بود.

**مقدار پیش‌فرض که شکل داده دارد.** برای گروه‌های بی‌معامله، زیرنویس کارت با فهرستی ثابت پر
می‌شد: «صنعتی» به «فولاد، مس، آلومینیوم». هر دو شاخه یک رشته فارسی سه‌بخشی تولید می‌کردند و
از بیرون تفکیک‌ناپذیر بودند. و چون آن سرویس «روز مؤثر معاملاتی» نداشت، **در هر تعطیلی کل
صفحه** یک تصویر منسجم و سراسر ساختگی می‌شد.

**رتبه‌ای که ویژگی فهرست است، نه موجودیت.** در نقشه حرارتی، دو گروه اول فهرست «داغ» برچسب
می‌خوردند؛ در روزی که کل بازار آرام بود، گروهی با حرارت ۰٫۱٪ هم «داغ» می‌شد.

**مرتب‌سازی روی ستونی که یکتا نیست.** «آخرین عرضه‌ها» بر اساس تاریخ شمسی بدون ساعت مرتب
می‌شد. در روزی با چهارصد عرضه، «ده تای اول» در دو فراخوانی می‌توانست دو مجموعه باشد.

**واحد ناهمگن در یک فهرست مرتب.** فهرست برترین عرضه‌کنندگان چنین درمی‌آمد: ۱٫۵ همت، ۶۵۳
میلیارد تومان، ۵۵۹ میلیارد تومان. هر سه درست، ولی خواننده باید بداند ۱٫۵ همت یعنی ۱۵۰۰
میلیارد تومان تا ترتیب را باور کند — در فهرستی که تمام معنایش ترتیب است.

نکته مشترک: **هیچ‌کدام باگ منطقی نبودند. همه فشرده‌سازی‌هایی بودند که برای نمایش معنا داشتند
و برای مصرف ماشینی نه.** یعنی همان کشف فصل پنجم، این بار نه در قالب یک تصمیم طراحی، که در
قالب فهرستی از اعداد غلط که سال‌ها منتشر شده بودند.

---

## فصل یازدهم — حذف، به‌عنوان تصمیم معماری

فصل چهارم با یک جمله تمام شد: ابزاری که پاسخ غلط می‌دهد از نبود ابزار بدتر است. حالا وقت عمل
به آن بود.

از چهل‌ودو ابزار، پنج‌تا بازنشسته شدند. هر پنج یک ویژگی مشترک داشتند که تا آخر ندیده بودمش:
**شکلشان را از ویجت گرفته بودند.** یکی فهرست را با ردیف‌های ساختگی تا سه کارت پر می‌کرد؛ یکی
رتبه‌بندی جایگاهی بود؛ یکی از جدولی می‌خواند که در محیط واقعی خالی بود و تنها پاسخ ممکنش
فهرست خالی بود — که مدل «چنین چیزی وجود ندارد» می‌خواندش.

قاعده‌ای که درآمد: **هر جا سرویسی «همیشه دقیقاً N ردیف» برمی‌گرداند، به آن مشکوک شو.** عدد
ثابت معمولاً از تعداد کارت‌های یک ویجت می‌آید، نه از داده.

در مقابل، ابزاری را نگه داشتم که جدولش هم خالی بود — مناقصه‌ها — چون جایگزینی نداشت. تفاوت
«ابزار زیان‌آور» و «ابزار بی‌داده» همین است: اولی باید حذف شود؛ دومی باید یاد بگیرد صادقانه
بگوید «این داده را نداریم» به‌جای «چنین چیزی وجود ندارد».

اگر بازنگری را با پرسش «کدام ابزار زیان‌آور است؟» پیش ببری، ابزار صرفاً خالی را هم همان‌طور
حذف می‌کنی.

---

## فصل دوازدهم — بازگشت به فرضیه اول: زبان، مسئله داده بود

ده فصل پس از آنکه فرضیه زبانی‌ام رد شد، مسئله زبان هنوز حل نشده بود.

کاربران فارسی‌زبانند و مدل تخصص فارسی ندارد. اول با پرامپت جنگیدم و هر بار قاعده را دقیق‌تر
کردم — و در راه فهمیدم قاعده «واژه انگلیسی ننویس» ناقص است: واژه‌های بیگانه‌ای که در
آزمون‌ها دیدم ایتالیایی، هلندی، لهستانی، آلمانی، اسپانیایی و حتی کره‌ای بودند. قاعده به
«هیچ واژه‌ای از هیچ زبانی جز فارسی» گسترش یافت.

ولی نقطه چرخش پرامپت نبود: **بیشتر نشت زبان از خود داده من می‌آمد.** وقتی خروجی کلیدی مثل
`measurementUnit` دارد، مدل آن را به‌عنوان سرستون جدول چاپ می‌کند. تقصیر مدل نیست؛ من
انگلیسی داده‌ام.

و اینجا معلوم شد فرضیه اول فقط غلط نبود؛ **دقیقاً برعکس** بود:

| | فرضیه اول می‌گفت | واقعیت |
|---|---|---|
| کجا؟ | در **ورودی مدل** — یعنی توصیف‌ها | در **خروجی ابزار** — یعنی کلیدهای داده |
| چه کنم؟ | `description` را فارسی کن | `description` را انگلیسی نگه دار |
| و آن یکی؟ | — | کلیدهایی که نهایتاً به کاربر می‌رسند را فارسی کن |

جهت درست فارسی‌سازی، معکوس چیزی بود که فرض کرده بودم. و مسئله از «متقاعد کردن مدل» به «اصلاح
قرارداد» منتقل شد — که همان قاعده فصل هشتم است: چیزی که در کد قطعی‌شدنی است، در پرامپت
دنبالش نگرد.

---

## فصل سیزدهم — سنجش: عددی که ماند

تا اینجا هر تغییری را با پنج‌شش پرسش دستی سنجیده بودم. برای اینکه بدانم مجموع کجا ایستاده،
به چیزی نیاز داشتم که خودم از قبل ساخته بودم بی‌آنکه بدانم به چه دردی می‌خورد: **پاسخ سرور
فهرست ابزارهای فراخوانی‌شده را هم برمی‌گرداند.** در ابتدا برای شفافیت رابط کاربری بود.

همان فیلد اجازه داد آزمونی بنویسم که برای هر ابزار یک پرسش فارسی واقعی می‌فرستد و سه چیز را
ثبت می‌کند: آیا ابزار درست انتخاب شد، پاسخ چه بود، و آیا واژه لاتینی مانده.

نتیجه آخرین اجرا: در **۳۱ مورد از ۳۷** ابزار موردنظر صدا زده شد، هر ۳۷ پاسخ بدون خطا برگشت،
**نشت زبانی صفر**.

آن شش مورد هم شکست نبودند. چهارتایشان پرسش خودم مبهم بود — «ورق گرم» گفته بودم و جستجو پنج
کالای هم‌نام برگرداند. مدل حدس نزد؛ ایستاد و پرسید کدام‌یک، دقیقاً همان‌طور که در توصیف ابزار
خواسته بودم.

و همان آزمون یک باگ پیدا کرد که مال خودم بود و تازه ساخته بودمش: «نسبت صفر» دو معنی داشت —
«عرضه‌ای نبوده» و «عرضه بوده ولی معامله‌ای نشده» — و من هر دو را یک پیام داده بودم. مدل هر
دو جمله را کنار هم چاپ کرد و پاسخ خودمتناقض شد. **همان اشتباهی که در فصل هفتم مستندش کرده
بودم، این بار حین رفع باگی دیگر از دست خودم درآمد.**

> **درس:** بازبینی ایستا جای آزمون روی مدل واقعی را نمی‌گیرد. خواندن کد نشان می‌دهد منطق چه
> می‌کند؛ فقط اجرا نشان می‌دهد مدل با آن چه می‌کند.

تحقیق هم همین را توصیه کرده بود: ارزیابی واقعی روی پرامپت‌های زبان هدف، نه روی ترجمه ماشینی.
تفاوت این بود که در عمل، **ارزانی و تکرارپذیری آزمون از دقتش مهم‌تر بود**. یک اسکریپت که
سی‌وهفت پرسش فارسی می‌فرستد و ابزارهای فراخوانی‌شده را ثبت می‌کند، بیش از هر بازبینی دستی به
کار آمد — چون می‌شد بعد از هر تغییر دوباره اجرایش کرد.

---

## فصل چهاردهم — ترازنامه

هر سطر یک گزاره است، منشأش، و سرنوشتی که پس از پیاده‌سازی پیدا کرد.

| گزاره | منشأ | سرنوشت | آنچه در عمل دیده شد |
|---|---|---|---|
| توصیف فارسی، کشف و انتخاب ابزار را برای کاربر فارسی بهتر می‌کند | ایده اولیه | **رد شد** | بنچمارک خلافش را نشان داد، و در عمل هم مشکل زبان اصلاً آنجا نبود |
| مسئله زبان با پرامپت و توصیف حل می‌شود | فرض ضمنی | **رد شد** | بیشتر نشت زبانی از کلیدهای انگلیسی خروجی می‌آمد؛ جهت معکوس بود (فصل ۱۲) |
| `annotations` و `title` راه‌حل چندزبانگی‌اند | فرضیه دوم | **اصلاح شد** | این‌ها کانال کاربر و کلاینت‌اند، نه کانال مدل. پاسخ درست به سؤالی که نپرسیده بودم (فصل ۵) |
| توصیف ابزار یک پرامپت زمان اجراست، نه مستند API | تحقیق | **تأیید شد** | هر بهبودی از بازنویسی متن به سبک دستورالعمل آمد، نه از دقیق‌تر کردن تایپ‌ها |
| نام ابزار باید فعل و شیء مشخص باشد، نه عمومی یا استعاری | تحقیق | **تأیید و تشدید شد** | نام غلط، توصیف درست را بی‌اثر می‌کند؛ غربال اول روی نام است (فصل ۶) |
| «چه زمانی نباید استفاده شود» را صریح بنویس | تحقیق | **مشروط شد** | مدل ضعیف قاعده منفی را به ممنوعیت عام تعمیم می‌دهد؛ باید در قالب مثبت نوشته شود (فصل ۸) |
| محدودیت‌ها و هشدارها را در `description` بنویس | تحقیق | **تنزل یافت** | هشدار متنی هشتاد تا نود درصد کار می‌کند. هر هشدار یعنی محتمل‌ترین خطا را می‌شناسی — پس در کد تشخیصش بده |
| پارامترهای مبهم منبع خطای بزرگ‌اند | تحقیق | **تأیید شد** | تاریخ رشته‌ای بدون الگو، خطای قالب را به «داده‌ای وجود ندارد» ترجمه می‌کرد (فصل ۷) |
| شناسه‌های ماشینی باید انگلیسی بمانند | تحقیق | **تأیید شد، با یک تله** | نقض لازم نیست عمدی باشد: یک مبدل نمایشی دوطرفه، «جستجوی کارگزار» را بی‌صدا به «جستجوی کالا» تبدیل کرد (فصل ۹) |
| شکست غالب چندزبانه، ناهماهنگی زبان مقدار پارامتر است | تحقیق | **تأیید شد** | راه‌حلش هم همان بود: نگاشت صریح واژه‌ها در توصیف پارامتر و اعتبارسنجی در کد |
| غنی کردن توصیف، دقت را کمی و هزینه را زیاد بالا می‌برد | تحقیق | **تأیید شد** | با ۴۲ ابزار، هزینه دو شکل داشت: توکن، و افت دقت انتخاب به‌خاطر شلوغی فضا (فصل ۴) |
| ارزیابی واقعی روی پرامپت زبان هدف لازم است | تحقیق | **تأیید شد و حیاتی بود** | آزمون سی‌وهفت پرسشی دو باگ پیدا کرد که بازبینی ایستا ندیده بود (فصل ۱۳) |
| پوشش کامل (هر متد یک ابزار) بهتر از مجموعه کوچک درشت‌دانه است | فرض پیاده‌سازی | **رد شد** | از ۴۲ ابزار پنج‌تا حذف شدند و نتیجه بهتر شد (فصل‌های ۴ و ۱۱) |
| کیفیت پاسخ عمدتاً تابع قدرت مدل است | فرض ضمنی | **رد شد** | با همان مدل رایگان و صرفاً بازطراحی ورودی، به دقتی رسیدم که ممکن نمی‌دانستم |
| خروجی ابزار می‌تواند همان مدل نمایشی موجود باشد | فرض ضمنی | **رد شد؛ یافته نو** | ویجت و مدل دو قرارداد متفاوت‌اند. اشتراک مدل داده صرفه‌جویی نیست؛ انتقال ابهام است (فصل ۵) |
| «نداریم»، «صفر» و «ورودی غلط» باید در خروجی از هم جدا شوند | — | **یافته نو** | پرتکرارترین الگوی پروژه، در چهار شکل مستقل (فصل ۷) |
| هر جا سرویسی «همیشه دقیقاً N ردیف» می‌دهد، شکلش از ویجت آمده | — | **یافته نو** | هر پنج ابزار بازنشسته این ویژگی را داشتند (فصل ۱۱) |
| قاعده سراسری، استثناهایش را نامرئی می‌کند | — | **یافته نو** | پس از هر اصلاح عمومی باید سراغ ابزارهایی رفت که مشمولش نشده‌اند (فصل ۹) |
| طرحواره MCP همیشه JSON است | پرسش باز | **تأیید شد** | JSON-RPC و JSON Schema؛ YAML فقط راحتی زمان توسعه است (پیوست الف) |
| Skill هم به JSON Schema نیاز دارد | پرسش باز | **رد شد** | Skill مبتنی بر Markdown است و انعطاف زبانی بیشتری دارد (پیوست الف) |

### سه اشتباهی که بیشترین درس را داشتند

**جهت فارسی‌سازی را برعکس گرفته بودم.** فرض کرده بودم باید ورودی مدل — توصیف‌ها — را فارسی
کنم. واقعیت این بود که توصیف‌ها باید انگلیسی بمانند و خروجی ابزارها فارسی شوند. اشتباهی که
بدون پیاده‌سازی معلوم نمی‌شد، چون در سطح نظری هر دو طرف قابل استدلال بودند.

**کانال کاربر را با کانال مدل اشتباه گرفته بودم.** `annotations` و `title` را راه‌حل انتخاب
ابزار دیدم، در حالی که اصلاً برای تعامل کاربر ساخته شده‌اند. همین خطا بعداً یک لایه پایین‌تر
تکرار شد، آنجا که خروجی ویجت را به مدل می‌دادم.

**قدرت مدل را متغیر اصلی فرض کرده بودم.** تا وقتی مدل را متغیر می‌دیدم، هر خطا توضیح آسانی
داشت و هیچ کاری لازم نبود جز «مدل بهتر». وقتی پذیرفتم ثابت است، تازه فهرست متغیرهای واقعی‌ام
پیدا شد — و همه‌شان در کنترل من بودند.

---

## فصل پانزدهم — قدم بعدی، و آنچه می‌ماند

مسئله امروز دیگر «درستی» نیست؛ **شکل** است. وقتی کاربر می‌پرسد «روند ارزش معاملات دو هفته
گذشته چطور بوده؟»، پاسخ درست یک پاراگراف نیست؛ یک نمودار است به‌علاوه یک جمله.

زیرساختش از قبل ساخته شده بود بی‌آنکه ارزشش را بدانم — همان‌طور که فیلد «ابزارهای
فراخوانی‌شده» بعداً تنها ابزار سنجشم شد. مدل کنار جمله روند، یک بلوک ساختاریافته هم تولید
می‌کند که رابط کاربری آن را به نمودار خطی تبدیل می‌کند. یعنی قرارداد از پیش اجازه پاسخ
غیرمتنی را می‌داد.

مسیر پیش رو از دید معماری یک تصمیم قرارداد است، نه ظاهر: پاسخ درباره یک کارگزار می‌تواند
همان کارت تحلیلی داشبورد باشد داخل گفت‌وگو؛ رتبه‌بندی ده عرضه‌کننده اگر مرتب‌شدنی و
کلیک‌شدنی باشد از ده خط متن مفیدتر است؛ و نمودار باید پاسخ **پیش‌فرض** هر پرسشی باشد که
«روند»، «مقایسه» یا «سهم» در آن هست.

یک ملاحظه امنیتی هم با این تغییر از «خوب است» به «واجب» تبدیل می‌شود: متن اخبار و اطلاعیه‌ها
نوشته شخص ثالث است. لحظه‌ای که رابط کاربری بلوک‌های ساختاریافته را رندر می‌کند، پاک‌سازی آن
متن‌ها تنها چیزی است که مانع می‌شود یک اطلاعیه، نموداری جعلی به صفحه تزریق کند. در سامانه‌ای
که خروجی‌اش عدد مالی است، این ریسک فرضی نیست.

### جمع‌بندی

اگر یک گزاره از این پانزده فصل باقی بماند، این است:

> **کیفیت پاسخ یک دستیار، بیش از آنکه تابع قدرت مدل باشد، تابع کیفیت قرارداد داده‌ای است که
> به او می‌دهیم.**

من با مدلی کار می‌کنم که در هیچ فهرستی جای برجسته‌ای ندارد. همان مدل، وقتی ورودی‌اش بازطراحی
شد، به دقتی رسید که پیش‌تر ممکن نمی‌دانستم. تفاوت، در متغیری بود که در کنترل من قرار داشت.

و توصیه عملی، که هم از تحقیق درآمد و هم از کد: **پیش از قضاوت پاسخ‌ها، ورودی مدل را بخوانید**
— همان JSON را، با چشم کسی که نه نام فیلدها را می‌شناسد، نه می‌داند کدام عدد ریال است و کدام
همت، و نه می‌داند `false` یعنی «نبود» یا «هنوز نه».

در تجربه من، بیشتر ایرادهایی که به مدل نسبت داده می‌شد، در همان فایل نوشته شده بود.

---
---

# پیوست‌ها

پنج پیوست زیر مطالب مرجع‌اند: چیزهایی که موقع نوشتن یک ابزار به آن‌ها رجوع می‌کنید، نه متنی
که از اول تا آخر خوانده شود.

---

## پیوست الف — نقشه کامل فیلدهای MCP و تصمیم زبان

### الف-۱. فیلدهای سطح ابزار

| فیلد | نوع | انگلیسی بماند؟ | توضیح |
|---|---|---|---|
| `name` | string | بله، مطلقاً | شناسه یکتا و ماشینی. مدل با این نام صدا می‌زند |
| `title` | string (اختیاری) | خیر؛ می‌تواند فارسی یا دوزبانه باشد | نام خوانا برای انسان و رابط کاربری |
| `description` | string (اختیاری) | ترجیحاً انگلیسی قوی | مهم‌ترین فیلد برای تصمیم مدل |
| `icons` | array | بی‌ربط به زبان | — |
| `annotations` | object | زیرفیلدها در جدول بعد | — |
| `inputSchema` | object | — | طرحواره ورودی |
| `outputSchema` | object (اختیاری) | — | قوانینش دقیقاً مثل `inputSchema` |

### الف-۲. داخل `annotations`

| فیلد | نوع | انگلیسی بماند؟ | توضیح |
|---|---|---|---|
| `title` | string | خیر | نسخه کوتاه‌تر نمایشی |
| `readOnlyHint` | boolean | — | ابزار اثر جانبی ندارد |
| `destructiveHint` | boolean | — | ابزار می‌تواند مخرب باشد |
| `idempotentHint` | boolean | — | فراخوانی مکرر با ورودی یکسان اثر اضافه ندارد |
| `openWorldHint` | boolean | — | ابزار با موجودیت‌های بیرونی و باز کار می‌کند |

یادآوری فصل پنجم: این فیلدها کانال کاربر و کلاینت‌اند، نه کانال مدل. `readOnlyHint` و
`destructiveHint` رفتار کلاینت را تغییر می‌دهند (مثلاً گرفتن تأیید)، نه دقت انتخاب ابزار را؛
و از منظر پروتکل «راهنما» محسوب می‌شوند، پس برای تصمیم‌های امنیتی سخت نباید به آن‌ها اتکای
مطلق کرد.

### الف-۳. داخل `inputSchema` و `outputSchema`

| بخش | انگلیسی بماند؟ | توضیح |
|---|---|---|
| `type` | بله (`"object"` در ریشه) | ثابت |
| کلیدهای `properties` | بله، مطلقاً | مدل با این نام‌ها پر می‌کند. هرگز `مکان` یا `شناسه_تیکت` |
| `description` هر پارامتر | ترجیحاً انگلیسی | جای نوشتن نگاشت واژه‌های فارسی |
| `enum` | بله، مقادیر انگلیسی | معادل فارسی را در `description` بنویس |
| `title` هر پارامتر | می‌تواند فارسی باشد | فقط نمایشی |
| `type`، `minimum`، `pattern` و بقیه | بله | مقادیر فنی استاندارد |
| `required` | بله | آرایه‌ای از نام‌های انگلیسی |
| `additionalProperties` | بله | معمولاً `false` |

### الف-۴. کلیدواژه‌های JSON Schema داخل هر Property

ریشه `inputSchema` همیشه باید `"type": "object"` باشد، اما داخل هر property تمام کلیدواژه‌های
زیر در دسترس‌اند.

**عمومی:** `type`، `description`، `title`، `default`، `enum`، `const`، `examples`،
`readOnly`، `writeOnly`، `deprecated`

**رشته:** `minLength`، `maxLength`، `pattern`، `format` (مقادیری مثل `email`، `uri`،
`date`، `date-time`، `time`، `uuid`، `regex`)

**عدد:** `minimum`، `maximum`، `exclusiveMinimum`، `exclusiveMaximum`، `multipleOf`

**آرایه:** `items`، `prefixItems`، `minItems`، `maxItems`، `uniqueItems`، `contains`،
`minContains`، `maxContains`

**شیء:** `properties`، `required`، `additionalProperties`، `minProperties`،
`maxProperties`، `propertyNames`، `patternProperties`، `dependentRequired`،
`dependentSchemas`

**ترکیب و شرط:** `allOf`، `anyOf`، `oneOf`، `not`، `if` / `then` / `else`

**ارجاع و متا:** `$ref`، `$defs`، `$anchor`، `$comment`

سه نمونه:

```json
"priority": {
  "type": "string",
  "title": "اولویت",
  "description": "Priority level. Map Persian words: کم → low, متوسط → medium, بالا/فوری → high, بحرانی → critical.",
  "enum": ["low", "medium", "high", "critical"],
  "default": "medium",
  "examples": ["high", "low"]
}
```

```json
"age": {
  "type": "integer",
  "description": "Age of the person in years.",
  "minimum": 0,
  "maximum": 120
}
```

```json
"tags": {
  "type": "array",
  "description": "List of tags.",
  "items": { "type": "string", "minLength": 1 },
  "minItems": 1,
  "maxItems": 10,
  "uniqueItems": true
}
```

### الف-۵. ساختار توصیه‌شده و یک نمونه کامل

```json
{
  "name": "english_snake_case_name",
  "title": "نام فارسی | English Name",
  "description": "English purpose + WHEN TO USE + WHEN NOT TO USE + limitations + returns",
  "annotations": {
    "title": "عنوان کوتاه فارسی",
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "param_name": {
        "type": "string",
        "title": "عنوان فارسی پارامتر",
        "description": "English explanation + Persian word mapping if needed",
        "enum": ["english_value_1", "english_value_2"]
      }
    },
    "required": ["param_name"],
    "additionalProperties": false
  }
}
```

```json
{
  "name": "convert_jalali_gregorian",
  "title": "تبدیل تاریخ شمسی و میلادی | Jalali to Gregorian",
  "description": "Converts dates between Jalali (Persian Solar Hijri) and Gregorian calendars. Use when the user mentions a Persian date or asks for calendar conversion. Accepts both Persian and English date formats. Do not use for the Hijri lunar calendar.",
  "annotations": {
    "title": "تبدیل تاریخ جلالی و میلادی",
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "The date to convert. Can be Jalali (e.g. '1403/06/12' or '۱۲ شهریور ۱۴۰۳') or Gregorian (e.g. '2024-09-03')."
      },
      "from_calendar": {
        "type": "string",
        "enum": ["jalali", "gregorian"],
        "description": "Source calendar. Map Persian terms: شمسی/جلالی → jalali, میلادی → gregorian."
      },
      "to_calendar": {
        "type": "string",
        "enum": ["jalali", "gregorian"],
        "description": "Target calendar."
      }
    },
    "required": ["date"]
  }
}
```

### الف-۶. آیا طرحواره همیشه JSON است؟

بله. `inputSchema` و `outputSchema` باید شیء JSON Schema باشند؛ MCP روی JSON-RPC کار می‌کند
و همه پیام‌ها — از جمله تعریف ابزارها — به‌صورت JSON رد و بدل می‌شوند. نسخه پیش‌فرض JSON
Schema در MCP نسخه ۲۰۲۰-۱۲ است.

| فرمت | وضعیت | توضیح |
|---|---|---|
| JSON | اجباری و استاندارد | تنها فرمتی که پروتکل می‌پذیرد |
| YAML | فقط راحتی توسعه‌دهنده | بعضی ابزارها اجازه می‌دهند طرحواره را YAML بنویسید و در زمان build به JSON تبدیل کنند؛ آنچه ارسال می‌شود JSON است |
| XML | استفاده نمی‌شود | پشتیبانی رسمی یا رایجی وجود ندارد |

### الف-۷. Skill در برابر Tool

| مورد | MCP Tool | Skill |
|---|---|---|
| فرمت اصلی | JSON Schema اجباری | معمولاً `SKILL.md` — مارک‌داون با frontmatter |
| هدف | فراخوانی ساختاریافته یک قابلیت | آموزش رفتار و روال کار به مدل |
| `inputSchema` / `outputSchema` | اجباری | ندارد یا اختیاری است |
| اجرای واقعی | مدل پارامترها را طبق طرحواره پر می‌کند و ابزار اجرا می‌شود | مدل دستورالعمل متنی را می‌خواند و دنبال می‌کند |
| حساسیت به زبان توصیف | بالا | انعطاف بیشتر؛ می‌تواند فارسی‌تر باشد |

در Skill، `name` معمولاً باید انگلیسی، با حروف کوچک و خط تیره باشد، و `description` مهم‌ترین
بخش برای کشف شدنش است. نیازی به پارامترهای ساختاریافته نیست، چون Skill دستورالعمل است نه
تابعی با ورودی ثابت.

---

## پیوست ب — کاتالوگ قبل و بعد

شش نمونه عمومی، برای مقایسه الگو. سه تای اول مسئله زبان را نشان می‌دهند و سه تای بعدی مسئله
ابهام پارامتر را.

### ب-۱. آب‌وهوا

**قبل**

```json
{
  "name": "get_weather",
  "description": "Gets the weather",
  "inputSchema": {
    "type": "object",
    "properties": {
      "location": { "type": "string", "description": "location" }
    }
  }
}
```

**بعد**

```json
{
  "name": "get_weather",
  "description": "Retrieves current weather conditions and short forecast for a given location. Use this when the user asks about temperature, rain, or weather in any language (including Persian). Always pass the location name in English or standard Latin transliteration (e.g. 'Tehran' not 'تهران'). Do not use for historical weather or long-term climate data.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "location": {
        "type": "string",
        "description": "City name or location. Prefer English (e.g. 'Isfahan', 'Shiraz'). If the user wrote it in Persian, convert to the common English form."
      },
      "units": {
        "type": "string",
        "enum": ["metric", "imperial"],
        "description": "Temperature units. Default to metric for Persian-speaking users."
      }
    },
    "required": ["location"]
  }
}
```

### ب-۲. جستجوی محصول

**قبل**

```json
{
  "name": "search_products",
  "description": "Search products in the catalog",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": { "type": "string" },
      "category": { "type": "string" }
    }
  }
}
```

**بعد**

```json
{
  "name": "search_products",
  "description": "Searches the product catalog by natural language query and optional category. Use when the user asks to find, look for, or check availability of products (works with Persian queries). Normalize the search query to English keywords when helpful for better matching. Do NOT use this for placing orders or checking order status.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search keywords. Can be in Persian or English. Example: 'لپ تاپ گیمینگ' or 'gaming laptop'."
      },
      "category": {
        "type": "string",
        "enum": ["electronics", "clothing", "home", "books", "other"],
        "description": "Product category in English. Map Persian category names to these values (e.g. 'الکترونیک' → 'electronics')."
      },
      "max_results": {
        "type": "integer",
        "description": "Maximum number of results to return. Default 10.",
        "minimum": 1,
        "maximum": 20,
        "default": 10
      }
    },
    "required": ["query"]
  }
}
```

### ب-۳. ایجاد تیکت پشتیبانی

**قبل**

```json
{
  "name": "create_support_ticket",
  "description": "Creates a support ticket",
  "inputSchema": {
    "type": "object",
    "properties": {
      "title": { "type": "string" },
      "description": { "type": "string" },
      "priority": { "type": "string" }
    }
  }
}
```

**بعد**

```json
{
  "name": "create_support_ticket",
  "description": "Creates a new customer support ticket. Use this when the user reports a problem, requests help, or wants to open a ticket (Persian or English). Keep the title and description in the user's original language (usually Persian). Do not use for checking existing ticket status.",
  "annotations": { "destructiveHint": false, "idempotentHint": false },
  "inputSchema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "Short title of the issue. Keep in the user's language (e.g. Persian)."
      },
      "description": {
        "type": "string",
        "description": "Detailed description of the problem. Preserve the original language and details provided by the user."
      },
      "priority": {
        "type": "string",
        "enum": ["low", "medium", "high", "critical"],
        "description": "Priority level. Map Persian terms: کم → low, متوسط → medium, بالا/فوری → high, بحرانی → critical."
      },
      "category": {
        "type": "string",
        "enum": ["technical", "billing", "account", "other"],
        "description": "Ticket category in English."
      }
    },
    "required": ["title", "description"]
  }
}
```

این نمونه دو رفتار متفاوت را کنار هم دارد و به همین دلیل مفید است: `title` و `description`
باید به زبان کاربر بمانند چون محتوای انسانی‌اند و بعداً انسان می‌خواندشان، ولی `priority` و
`category` باید به مقدار انگلیسی ثابت نگاشت شوند. اگر این تفاوت صریح نوشته نشود، همان
ناهماهنگی زبان مقدار پارامتر رخ می‌دهد که در فصل دوم توصیفش کردم.

### ب-۴. تبدیل تاریخ

**قبل**

```json
{ "name": "convert_date", "description": "Converts dates between calendars" }
```

**بعد:** نمونه کامل در بند الف-۵ آمده است.

### ب-۵. قیمت‌های تاریخی سهم

**قبل**

```json
{
  "name": "get_historical_stock_prices",
  "description": "Gets historical stock prices.",
  "parameters": {
    "ticker": { "type": "string" },
    "period": { "type": "string" },
    "start":  { "type": "string" },
    "end":    { "type": "string" }
  }
}
```

مدل نمی‌داند `start` و `end` چه فرمتی دارند، پس بازه چندساله می‌گیرد و توکن می‌سوزاند.

**بعد**

```json
{
  "name": "get_historical_stock_prices",
  "description": "Retrieves daily closing prices for a stock within a specified date range.\n\nWHEN TO USE: Use when the user asks for historical stock performance, price history, or trends over a specific time window.\n\nCONSTRAINTS & RULES:\n1. Dates MUST be in YYYY-MM-DD format.\n2. Do not use 'period' if 'start_date' and 'end_date' are provided.\n3. Weekends and holidays return empty data.\n4. Maximum allowed range per request is 365 days.\n\nRETURNS: A JSON array of objects, each containing: {date: string, close: number, volume: integer}.",
  "parameters": {
    "type": "object",
    "properties": {
      "ticker": {
        "type": "string",
        "description": "Stock ticker symbol in uppercase. Example: 'AAPL', 'MSFT'."
      },
      "start_date": {
        "type": "string",
        "description": "Start date (inclusive) in YYYY-MM-DD format. Example: '2026-01-15'."
      },
      "end_date": {
        "type": "string",
        "description": "End date (inclusive) in YYYY-MM-DD format. Must be >= start_date."
      }
    },
    "required": ["ticker", "start_date", "end_date"]
  }
}
```

### ب-۶. پارامتر `object` مبهم

**قبل**

```json
{
  "name": "search_tickets",
  "description": "Search tickets with filter object.",
  "parameters": { "filter": { "type": "object" } }
}
```

مدل قالب `filter` را حدس می‌زند — گاهی `{"status": "open"}`، گاهی
`{"where": {"status": "open"}}` — و خطای پارس رخ می‌دهد.

**بعد**

```json
{
  "name": "search_tickets",
  "description": "Searches support tickets matching specific criteria.\n\nFILTER FORMAT:\nThe 'filter' parameter accepts a JSON object with explicit 'property', 'operator', and 'value' keys.\nSupported operators: 'Equal', 'NotEqual', 'GreaterThan', 'LessThan'.\n\nEXAMPLE:\nfilter: {'property': 'status', 'operator': 'Equal', 'value': 'open'}\n\nLIMITATIONS:\nFor full-text message search, use 'search_ticket_messages' instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "filter": {
        "type": "object",
        "description": "Structured condition object. Example: {'property': 'priority', 'operator': 'Equal', 'value': 'high'}"
      },
      "limit": {
        "type": "integer",
        "description": "Number of items (1-100). Default: 10. Higher limits increase context usage.",
        "default": 10
      }
    },
    "required": ["filter"]
  }
}
```

### ب-۷. الگوی کلی تغییرات

| جنبه | قبل | بعد |
|---|---|---|
| زبان `description` | فقط فارسی، یا انگلیسی خیلی کوتاه | انگلیسی دقیق با اشاره به پشتیبانی از فارسی |
| شرط استفاده و عدم استفاده | وجود ندارد | صریح نوشته شده |
| پارامترها | توضیح مبهم | مثال، زبان موردانتظار، و نگاشت واژه‌های فارسی |
| `enum` | آزاد یا بدون توضیح | مقدار انگلیسی ثابت با راهنمای نگاشت |
| نام ابزار | گاهی مبهم یا عمومی | همیشه انگلیسی و فعل‌محور |
| خروجی | نامشخص | ساختار و فیلدهایش تصریح شده |

---

## پیوست ج — چک‌لیست پیش از انتشار

### زبان

- `name`، کلیدهای `properties`، مقادیر `enum` و `required` همگی انگلیسی‌اند.
- `description` انگلیسی باکیفیت است؛ نه ترجمه ماشینی، نه فارسی.
- برای هر پارامتر متن آزاد، زبان موردانتظار مقدار صریح نوشته شده.
- نگاشت واژه‌های فارسی به مقادیر `enum` در `description` آمده.
- `title` و `annotations.title` برای کاربر فارسی پر شده‌اند، با آگاهی از اینکه اثری بر انتخاب
  ابزار ندارند.
- هیچ کلید انگلیسی‌ای در خروجی نمانده که مستقیم به کاربر نمایش داده شود.

### نام و مرزها

- نام ابزار ترکیب فعل و شیء مشخص است و معنای عرفی‌اش با کارش یکی است.
- ابزارهای همسایه از هم تفکیک شده‌اند، ترجیحاً با جمله مثبت نه با نهی عام.
- هیچ ابزاری با نام ساختگی یا مفهومی که در کسب‌وکار وجود ندارد باقی نمانده.

### خروجی

- خروجی مدل ارائه ویجت نیست؛ قرارداد جداگانه‌ای برای مدل دارد.
- فیلدهای ظاهری با قاعده عام حذف شده‌اند، نه با فهرست نمونه‌ها؛ و قاعده عام روی موارد مشابه
  واقعی آزمایش شده تا چیزی را که نباید نگیرد.
- برچسب و مقدار در دو فیلد جدا نیستند؛ برچسب خود کلید است.
- هیچ فیلدی معنایش به مقدار فیلد دیگری وابسته نیست.
- واحد داخل مقدار است و در یک فهرست مرتب، واحدها همگن‌اند.
- همراه هر نام موجودیت، شناسه یا نشانی‌اش هم می‌آید، و آن شناسه واقعاً به همان موجودیت اشاره
  می‌کند.
- «نداریم»، «صفر» و «ورودی نامعتبر» سه پیام متمایز دارند.
- وجود موجودیت پیش از خالی بودن نتیجه بررسی می‌شود.
- هیچ فیلدی مقدار پیش‌فرض حدسی ندارد؛ آنچه معلوم نیست حذف می‌شود.
- هیچ سرویسی «همیشه دقیقاً N ردیف» برنمی‌گرداند، مگر آنکه خود داده این‌طور باشد.
- مرتب‌سازی روی ستونی یکتا یا با شکن تساوی انجام می‌شود.

### ارزیابی

- پاسخ سرور فهرست ابزارهای فراخوانی‌شده را برمی‌گرداند؛ بدون این، سنجش ممکن نیست.
- برای هر ابزار دست‌کم یک پرسش واقعی زبان کاربر در مجموعه آزمون هست.
- آزمون سه چیز را ثبت می‌کند: ابزار انتخاب‌شده، خطا، و نشت زبانی.
- پس از هر اصلاح سراسری، ابزارهایی که مشمول آن نشده‌اند جداگانه بازبینی شده‌اند.

---

## پیوست د — پرامپت ارزیابی خودکار

پیش از انتشار سرور دو راه ارزیابی دارید. اول، ارزیابی تعاملی با MCP Inspector برای صحت‌سنجی
طرحواره‌ها و تست دستی. دوم، دادن تعریف ابزار به یک مدل قوی با پرامپت زیر.

```
You are an expert AI Engineer specializing in Model Context Protocol (MCP) tool design and Agentic Systems.
Your task is to evaluate a given MCP Tool Definition against 6 core quality dimensions
to identify "Tool Description Smells" and provide actionable recommendations.

### EVALUATION RUBRIC (Score each dimension from 1 to 5):

1. **Clear Purpose (weight: 20%)**
   - Score 1: Vague or missing purpose (e.g., "process_data").
   - Score 5: Explicit action + object definition (e.g., "Creates a high-priority refund ticket for duplicated charges").

2. **Usage Guidelines & Trigger Conditions (weight: 20%)**
   - Score 1: No indication of when to call or not call this tool.
   - Score 5: Explicit "WHEN TO USE" and "WHEN NOT TO USE" statements including sibling tool disambiguation.

3. **Unstated Limitations & Boundaries (weight: 20%)**
   - Score 1: Fails to state rates, bounds, date ranges, or prerequisite steps.
   - Score 5: Explicit boundary rules (e.g., "Max date range 365 days; requires admin authentication").

4. **Parameter Clarity & Formats (weight: 15%)**
   - Score 1: Opaque parameters with raw 'string' or 'object' types lacking format details.
   - Score 5: Every parameter specifies exact format, constraints, enums, and default values (e.g., "YYYY-MM-DD").

5. **Exemplars & Concrete Examples (weight: 10%)**
   - Score 1: No examples or misleading/invalid examples.
   - Score 5: Concise, valid JSON example(s) demonstrating expected parameter shapes and edge cases.

6. **Output Format Specification (weight: 15%)**
   - Score 1: No description of returned payload.
   - Score 5: Detailed description of output data structure, status codes, and error shapes.

---

### INPUT TOOL DEFINITION TO EVALUATE:
[Insert your JSON Tool Schema here]

---

### EXPECTED OUTPUT FORMAT:
Provide your evaluation in structured Markdown:
1. **Score Breakdown Table:** Dimension, Score (1-5), Identified Smells.
2. **Overall Quality Index (OQI):** Weighted Average Score / 5.
3. **Critical Vulnerabilities & Smells Identified:** Bulleted list of reasons for lost points.
4. **Refactored MCP Tool Schema:** The complete, fully-augmented and smell-free JSON definition.
```

معیار پذیرشی که در منابع آمده این است: اگر امتیاز کل زیر ۳٫۸ از ۵ باشد، احتمال رفتار
غیرمنتظره و عدم فراخوانی در محیط واقعی بالای هفتاد درصد است.

دو ملاحظه از تجربه این پروژه:

**یک.** این ارزیابی ایستاست. همان‌طور که فصل سیزدهم نشان داد، بازبینی ایستا جای آزمون روی مدل
واقعی را نمی‌گیرد. آن را مکمل بدانید، نه جایگزین.

**دو.** این چارچوب فقط **تعریف ابزار** را می‌سنجد. نیمی از مشکلات واقعی من در **خروجی** ابزار
بود که این پرامپت اصلاً نمی‌بیندش. برای خروجی، از چک‌لیست پیوست ج استفاده کنید.

---

## پیوست ه — واژه‌نامه

| اصطلاح | توضیح |
|---|---|
| MCP (Model Context Protocol) | پروتکل زمینه مدل؛ استانداردی برای اتصال مدل به ابزارها و داده‌ها، مبتنی بر JSON-RPC |
| Tool | ابزار؛ تابعی که مدل خودش تصمیم به فراخوانی‌اش می‌گیرد |
| Resource | منبع؛ داده فقط‌خواندنی که کلاینت یا کاربر آن را وارد می‌کند |
| Prompt (پریمیتیو) | قالب پرامپت از پیش آماده که کاربر انتخابش می‌کند |
| Tool Description Smell | ایراد کیفی در توصیف ابزار که به خطای رفتاری منجر می‌شود |
| عدم فراخوانی (Undercalling) | فراخوانی نشدن ابزار در جایی که باید فراخوانی می‌شد |
| سوءاستفاده با اطمینان (Confident Misuse) | فراخوانی درست با پارامترهای بدقالب |
| انتخاب ابزار اشتباه | انتخاب نادرست میان ابزارهای مشابه |
| تفکیک ابزار همسایه (Sibling Disambiguation) | جدا کردن صریح یک ابزار از ابزارهای مشابهش |
| ناهماهنگی زبان مقدار پارامتر | تولید مقدار به زبان کاربر در حالی که بک‌اند انگلیسی می‌خواهد |
| جریمه زبانی انباشته | افت مضاعف وقتی هم پرامپت و هم توصیف غیرانگلیسی‌اند |
| مدل ارائه (Presentation Model) | شیء داده‌ای که برای رندر یک ویجت طراحی شده |
| MCP Inspector | ابزار رسمی بررسی و تست دستی سرورهای MCP |

---

> **درباره منابع این متن:** این نوشته تجمیع سه سند کاری است — تحقیق مبنایی درباره طراحی
> توصیف ابزار در MCP، تحقیق متمرکز روی چالش زبان کاربر در برابر زبان توصیف، و گزارش
> پیاده‌سازی دستیار گفت‌وگو روی داده‌های بورس کالای ایران. درصدها و اعداد بنچمارکی از منابع
> مرحله تحقیق نقل شده‌اند؛ اعداد مربوط به پروژه حاصل اندازه‌گیری مستقیم‌اند.
